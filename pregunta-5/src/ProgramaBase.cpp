#include "../headers/ProgramaBase.hpp"

// Constructor de la clase ProgramaBase
// Parámetros:
// - lenguajeBase: El lenguaje base en el que está escrito el programa.
ProgramaBase::ProgramaBase(const string& lenguajeBase) : lenguajeBase(lenguajeBase) {
    // El constructor inicializa el atributo lenguajeBase con el valor proporcionado.
}
